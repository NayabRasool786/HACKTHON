<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher's Research Projects</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <h1>Teacher's Research Projects</h1>
    </header>
    <main>
        <% if (projects && projects.length > 0) { %>
            <ul class="project-list">
                <% projects.forEach(project => { %>
                    <li class="project-item">
                        <h2><%= project.title %></h2>
                        <p><strong>Year:</strong> <%= project.year %></p>
                        <p><strong>Description:</strong> <%= project.description %></p>
                        <% if (project.collaborators && project.collaborators.length > 0) { %>
                            <p><strong>Collaborators:</strong> <%= project.collaborators.join(', ') %></p>
                        <% } %>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No research projects found for this teacher in the last four years.</p>
        <% } %>
    </main>
    <footer>
        <p>&copy; <%= new Date().getFullYear() %> School Name. All rights reserved.</p>
    </footer>
    <script src="/js/main.js"></script>
</body>
</html>
